type Subscription {
  onTextUpdate: TextUpdate @aws_subscribe(mutations: ["updateText"])
}

type Mutation {
  updateText(text: String!): TextUpdate
}

type TextUpdate {
  id: ID!
  text: String!
  timestamp: AWSTimestamp!
}

type Query {
  getText: TextUpdate
}
